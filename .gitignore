# Personal data — never share or commit. This is the whole tracker:
# the SQLite database and every stored receipt live here.
data/

# Exported backups — same sensitive data (ledger, receipts, rationale) in a
# portable form. Never belongs in the repo, wherever it lands.
*.zip
HSA-*.csv
HSA-*.json
HSA-*.pdf
HSA-full-backup*
HSA-receipts*

node_modules/
.DS_Store
*.log

# Build output — rebuilt with npm run installer; too large for git
dist/

# Personal seed history — never belongs in the repo
src/seed-data.json

# Private agent context — machine-local, never commit
CLAUDE.local.md
